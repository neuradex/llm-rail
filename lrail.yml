mode: enforce
default: allow
rules:
  - effect: deny
    commands:
      - "rm -rf *"
      - "sudo *"
      - "chmod 777 *"
      - "git push --force *"
      - "git push -f *"
      - "git reset --hard *"
